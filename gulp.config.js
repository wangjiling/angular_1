"use strict";

/*build config*/
var FileConfig  = {
    /* 打包输出位置 */
    build : {
        rootPath: 'build',
        view: 'build/views/',
        css: 'build/css/',
        js: 'build/js/',
        img: 'build/img/'
    }
};

module.exports = FileConfig;
